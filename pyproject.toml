[project]
name = "ai-agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "HeadTarun", email = "tarundangeengineer@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "python-dotenv==1.1.1",
    "supabase==2.31.0",
]

[project.scripts]
ai-agents = "ai_agents:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
